table! {
    appuser (id) {
        id -> Int4,
        username -> Varchar,
        password -> Varchar,
        detail -> Varchar,
        email -> Varchar,
        firstname -> Varchar,
        lastname -> Varchar,
    }
}
